<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AndroidWorkshopApp" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.AndroidWorkshopApp.SplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation
        duration. -->
        <item name="windowSplashScreenBackground">@color/launcher_background_color</item>

        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <item name="postSplashScreenTheme">@style/Theme.AndroidWorkshopApp</item>
    </style>
</resources>
